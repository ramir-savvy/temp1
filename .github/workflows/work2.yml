name: work2

concurrency: ${{ github.workflow }}-${{ github.ref }}
on:
  workflow_dispatch:
  push:
#comment3
jobs:
  sleepjob:
    runs-on: ubuntu-latest
    steps:
      - name: Sleep
        run: |
          echo "github.workflow: $GITHUB_WORKFLOW"
          echo sleeping...
          sleep 60
